<UserControl x:Class="Vanilla_RTX_App.Core.ReviewPromptControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             IsTextScaleFactorEnabled="False"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <!-- Semi-transparent overlay that blocks all interaction - clicking dismisses (show later) -->
    <Grid x:Name="RootGrid"
          Background="#C0000000"
          Visibility="Collapsed"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          Tapped="RootGrid_Tapped">

        <!-- Dialog Card -->
        <Border x:Name="DialogBorder"
                Padding="24"
                Margin="24"
                CornerRadius="8"
                Background="{ThemeResource SolidBackgroundFillColorBase}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Tapped="DialogBorder_Tapped"
                Translation="0,0,256">
            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>

            <StackPanel Spacing="16">
                <!-- Header Text -->
                <TextBlock FontSize="14"
                           TextWrapping="Wrap"
                           LineHeight="20"
                           TextAlignment="Center"
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}">
                    <Run Text="A message from the developer:&#x0a;&#x0a;"
                         FontSize="14"/>
                    <Run Text="If you've enjoyed the app or have found it helpful so far, &#x0a;you can sponser me through my"/>
                    <Hyperlink UnderlineStyle="Single"
                               NavigateUri="https://ko-fi.com/cubeir">
                        <Run Text="Ko-Fi page."
                             FontWeight="SemiBold"/>
                    </Hyperlink>
                    <Run Text="&#x0a;...or simply by leaving an honest review!&#x0a;Anything helps, and encourages me to continue my projects." />
                </TextBlock>

                <!-- Button Row -->
                <StackPanel Orientation="Horizontal"
                            Spacing="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="0,8,0,0">
                    <Button x:Name="ReviewButton"
                            Content="Leave an honest review ⭐"
                            Padding="16,8"
                            MinWidth="130"
                            Translation="0,0,24">
                        <Button.Shadow>
                            <ThemeShadow />
                        </Button.Shadow>
                    </Button>

                    <Button x:Name="LaterButton"
                            Content="Show later"
                            Padding="16,8"
                            MinWidth="100"
                            Translation="0,0,24">
                        <Button.Shadow>
                            <ThemeShadow />
                        </Button.Shadow>
                    </Button>

                    <Button x:Name="NeverButton"
                            Content="Don't show again"
                            Padding="16,8"
                            MinWidth="140"
                            Translation="0,0,24">
                        <Button.Shadow>
                            <ThemeShadow />
                        </Button.Shadow>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
