<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Vanilla_RTX_App.Modules.Alchitex.Alchitex"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Vanilla_RTX_App.Modules.Alchitex"
        SizeChanged="Window_SizeChanged"
        Title="Alchitex">

    <!-- License gate uses Acrylic; MainGrid uses Mica (set in code-behind via SystemBackdrop swap) -->
    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!--  BACKGROUND IMAGE  (behind everything, incl. titlebar)  -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Image x:Name="AlchitexBackground"
               Source="ms-appx:///Modules/Alchitex/Assets/huge_background.png"
               Stretch="None"
               HorizontalAlignment="Center"
               VerticalAlignment="Bottom"
               IsHitTestVisible="False"
               CacheMode="BitmapCache"
               Canvas.ZIndex="-2"
               Opacity="1.0">
        </Image>

        <Image x:Name="RedstoneLayer"
               HorizontalAlignment="Center"
               Stretch="None"
               VerticalAlignment="Top"
               IsHitTestVisible="False"
               Canvas.ZIndex="-3"
               CacheMode="BitmapCache" />

        <!-- Light theme tint overlay — identical to MainWindow -->
        <Border x:Name="LightThemeOverlay"
                Grid.ColumnSpan="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                IsHitTestVisible="False"
                Visibility="{ThemeResource LightThemeBackgroundVisibility}"
                Canvas.ZIndex="-1">
            <Border.Background>
                <SolidColorBrush Color="#17373737" />
            </Border.Background>
        </Border>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!--  SHARED TITLEBAR  (always on top, always visible)       -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Grid x:Name="TitleBarHost"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Top"
              Height="35"
              Canvas.ZIndex="100">

            <!-- Shadow receiver behind the titlebar -->
            <Grid x:Name="TitleBarShadowReceiver" />

            <!-- Styled titlebar strip -->
            <Border x:Name="TitleBarBackground"
                    Height="37"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    Background="{ThemeResource CardBackgroundFillColorSecondary}"
                    BorderBrush="{ThemeResource HyperlinkButtonBorderThemeBrush}"
                    BorderThickness="0,0,0,6"
                    CornerRadius="0"
                    Translation="0,0,24"
                    IsHitTestVisible="False">
                <Border.Shadow>
                    <ThemeShadow x:Name="TitleBarShadow" />
                </Border.Shadow>
            </Border>

            <!-- Drag area (leaves room for the Info button on the left in main state;
                 full-width during license — we swap SetTitleBar target in code-behind) -->
            <Border x:Name="TitleBarDragAreaFull"
                    HorizontalAlignment="Stretch"
                    Height="31"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    IsHitTestVisible="False" />

            <!-- Narrowed drag area used when the Info button is showing -->
            <Border x:Name="TitleBarDragAreaNarrow"
                    Margin="50,0,0,0"
                    Height="31"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    IsHitTestVisible="False"
                    Visibility="Collapsed" />

            <!-- Info button — only visible in main content state -->
            <Button x:Name="InfoButton"
                    Width="40"
                    Height="31"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    CornerRadius="0"
                    Padding="0"
                    Visibility="Collapsed"
                    ToolTipService.ToolTip="Learn more..."
                    Click="InfoButton_Click">
                <FontIcon Glyph="&#xE946;"
                          FontSize="14"
                          IsTextScaleFactorEnabled="False" />
            </Button>

            <!-- Centered logo + title pair (single instance, shared by all panels) -->
            <Grid x:Name="TitleBarCenterContent"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Margin="0,-1,-1,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Logo canvas -->
                <Canvas Grid.Column="0"
                        Height="20"
                        Width="20"
                        VerticalAlignment="Center"
                        Margin="0,0,8,0"
                        CacheMode="BitmapCache"
                        Canvas.ZIndex="1">

                    <!-- Halo (bottom layer) -->
                    <Image x:Name="AlchitexIconHalo"
                           Source="ms-appx:///Modules/Alchitex/Assets/logo.halo.png"
                           Height="128"
                           Width="128"
                           Canvas.Left="-54"
                           Canvas.Top="-54"
                           Opacity="0.25"
                           Canvas.ZIndex="2"
                           IsHitTestVisible="False"
                           CacheMode="BitmapCache" />

                    <!-- Base "On" image -->
                    <Image x:Name="AlchitexIconOn"
                           Source="ms-appx:///Modules/Alchitex/Assets/logo.on.png"
                           Height="20"
                           Width="20"
                           Canvas.Top="1"
                           Opacity="1.0"
                           Canvas.ZIndex="3"
                           IsHitTestVisible="False"
                           CacheMode="BitmapCache" />

                    <!-- "Off" overlay (fades in/out during animation) -->
                    <Image x:Name="AlchitexIconOff"
                           Source="ms-appx:///Modules/Alchitex/Assets/logo.off.png"
                           Height="20"
                           Width="20"
                           Canvas.Top="1"
                           Opacity="0.0"
                           Canvas.ZIndex="4"
                           IsHitTestVisible="False"
                           CacheMode="BitmapCache" />

                    <!-- "Super" overlay -->
                    <Image x:Name="AlchitexIconSuper"
                           Source="ms-appx:///Modules/Alchitex/Assets/logo.super.png"
                           Height="20"
                           Width="20"
                           Canvas.Top="1"
                           Opacity="0.0"
                           Canvas.ZIndex="5"
                           IsHitTestVisible="False"
                           CacheMode="BitmapCache" />

                    <!-- Button to make lamp interactive -->
                    <Button x:Name="LogoInteractButton"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Canvas.Left="0.5"
                            Canvas.Top="1"
                            Opacity="0.0"
                            Height="20"
                            Width="20"
                            Canvas.ZIndex="5"
                            CornerRadius="0"
                            Click="LogoInteractButton_Click" />
                </Canvas>

                <!-- Window title text — updated in code-behind later! -->
                <TextBlock x:Name="TitleBarText"
                           Text="Alchitex  |  License Agreement"
                           Grid.Column="1"
                           Margin="0,6,0,5"
                           VerticalAlignment="Center"
                           FontWeight="Normal"
                           IsTextScaleFactorEnabled="False"
                           IsHitTestVisible="False" />
            </Grid>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!--  LOADING PANEL                                          -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Grid x:Name="LoadingPanel"
              Visibility="Visible"
              Margin="0,35,0,0"
              Background="Transparent">
            <ProgressRing IsActive="True"
                          Width="100"
                          Height="100"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center" />
        </Grid>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!--  LICENSE PANEL                                          -->
        <!--  Sits BELOW the shared titlebar (Margin top = 35)       -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Grid x:Name="LicensePanel"
              Visibility="Collapsed"
              Background="{ThemeResource OverlayBackgroundColor}"
              Padding="16, 0, 16 16"
              Margin="0,36,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <!-- license text  -->
                <RowDefinition Height="Auto" />
                <!-- buttons       -->
            </Grid.RowDefinitions>

            <!-- Shadow receiver (behind everything in this panel) -->
            <Grid x:Name="LicenseShadowReceiver"
                  Grid.RowSpan="2" />

            <!-- ── License RichTextBlock ── -->
            <ScrollViewer Grid.Row="0"
                          Background="{ThemeResource OverlayBackgroundColor}"
                          Margin="64,32,64,32"
                          MaxWidth="720"
                          BorderBrush="#40FFFFFF"
                          BorderThickness="2"
                          CornerRadius="12"
                          VerticalScrollBarVisibility="Auto">
                <RichTextBlock x:Name="LicenseTextBlock"
                               IsTextSelectionEnabled="False"
                               Padding="24"
                               TextWrapping="Wrap"
                               IsTabStop="False"
                               FontSize="14"
                               LineHeight="20"
                               IsTextScaleFactorEnabled="False"
                               HorizontalAlignment="Center"
                               Translation="0,0,32">
                    <RichTextBlock.Shadow>
                        <ThemeShadow x:Name="LicenseTextShadow" />
                    </RichTextBlock.Shadow>
                    <!-- Populated in code-behind -->
                </RichTextBlock>
            </ScrollViewer>

            <!-- ── Action buttons ── -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Spacing="16"
                        Background="Transparent"
                        Margin="0,0,0,24">

                <Button x:Name="DisagreeButton"
                        Padding="16,16,16,16"
                        IsTextScaleFactorEnabled="False"
                        Translation="0,0,32"
                        Click="DisagreeButton_Click"
                        Content="Disagree and close"
                        Style="{ThemeResource ButtonRevealStyle}">
                    <Button.Shadow>
                        <ThemeShadow x:Name="DisagreeShadow" />
                    </Button.Shadow>

                </Button>

                <Button x:Name="AgreeButton"
                        Padding="16,16,16,16"
                        Style="{StaticResource AccentButtonStyle}"
                        IsTextScaleFactorEnabled="False"
                        Translation="0,0,32"
                        Content="I have read and agreed to the terms and conditions of this feature"
                        Click="AgreeButton_Click">
                    <Button.Shadow>
                        <ThemeShadow x:Name="AgreeShadow" />
                    </Button.Shadow>
                </Button>

            </StackPanel>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!--  MAIN CONTENT PANEL (Shown after license accept)  -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Grid x:Name="MainGrid"
              Visibility="Collapsed"
              Background="Transparent"
              Margin="0,35,0,0">

            <!-- Shadow receiver (behind everything) -->
            <Grid x:Name="MainShadowReceiver" />

            <!-- ── Alchitex feature content area ── -->

            
            <Button Content="The redstone circuits are still being laid down. Keep an eye out for updates!"
                    x:Name="AnnouncementButton"
                    Click="AnnouncementButton_Click"
                    Opacity="0.85"
                    Padding="6"
                    CornerRadius="0"
                    FontSize="14"
                    VerticalAlignment="Center"
                    MinHeight="64"
                    Foreground="White"
                    Margin="-0.5"
                    HorizontalAlignment="Stretch"
                    Canvas.ZIndex="998">
                <Button.Background>
                    <AcrylicBrush TintColor="{ThemeResource SystemAltHighColor}"
                                  TintOpacity="0.0"
                                  FallbackColor="{ThemeResource SystemAccentColor}" />
                </Button.Background>
                </Button>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════ -->
        <!--  SPLASH OVERLAY  (covers the entire window)             -->
        <!-- ═══════════════════════════════════════════════════════ -->
        <Grid x:Name="SplashOverlay"
              Visibility="Collapsed"
              Canvas.ZIndex="9999">
            <Grid.Background>
                <SolidColorBrush Color="{ThemeResource SolidBackgroundFillColorBase}" />
            </Grid.Background>

            <Viewbox HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch"
                     Margin="208,164,208,164"
                     Stretch="Uniform">
                <Grid Width="256"
                      Height="256">

                    <!-- Halo — bottom layer -->
                    <Image x:Name="SplashLogoHalo"
                           Source="ms-appx:///Modules/Alchitex/Assets/splash.halo.png"
                           Width="1280"
                           Height="1280"
                           Margin="-512,-512,-512,-512"
                           Opacity="0.175"
                           Stretch="Uniform"
                           Canvas.ZIndex="1"
                           CacheMode="BitmapCache" />

                    <!-- Base "On" — middle layer -->
                    <Image x:Name="SplashLogo"
                           Source="ms-appx:///Modules/Alchitex/Assets/splash.on.png"
                           Width="192"
                           Height="192"
                           Opacity="1.0"
                           Stretch="Uniform"
                           Canvas.ZIndex="2"
                           CacheMode="BitmapCache" />

                    <!-- "Off" overlay — used by animator -->
                    <Image x:Name="SplashLogoOff"
                           Source="ms-appx:///Modules/Alchitex/Assets/splash.off.png"
                           Width="192"
                           Height="192"
                           Opacity="0.0"
                           Stretch="Uniform"
                           Canvas.ZIndex="3"
                           CacheMode="BitmapCache" />

                    <!-- "Super" overlay — top layer -->
                    <Image x:Name="SplashLogoSuper"
                           Source="ms-appx:///Modules/Alchitex/Assets/splash.super.png"
                           Width="192"
                           Height="192"
                           Opacity="0.0"
                           Stretch="Uniform"
                           Canvas.ZIndex="4"
                           CacheMode="BitmapCache" />
                </Grid>
            </Viewbox>
        </Grid>

    </Grid>
</Window>
